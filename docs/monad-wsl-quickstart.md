# Monad WSL Quickstart

This guide helps Windows users set up a Linux-like environment using **WSL (Windows Subsystem for Linux)** to run and experiment with the Monad community tools.

---

## 1) Install WSL + Ubuntu
Open **PowerShell as Administrator** and run:
```powershell
wsl --install -d Ubuntu

